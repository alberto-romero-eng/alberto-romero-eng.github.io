MINIKUBE INSTALL, KUBERNETES -- updated: 2023-02-06 19:23

(1) Install Windows Subsystem for Linux (WSL).  Command checks:

where wsl

wsl --help

wsl --list

wsl --list --running

wsl --update



(2) Install Docker Desktop



(3) Get Minikube installer:

where winget

winget install minikube

There are other options (exe installer, chocolatey, etc)

Several files should be downloaded (400MB, 1.5GB), progress indicator may act faulty.

Check download activity with TaskManager, Network column.



(4) Config and run Minikube the first time:

where minikube

minikube help|status

minikube delete

minikube config set driver docker

minikube start|stop

minikube start --driver=docker # optional



(5) Check kubectl is available:

where kubectl



(6) TODO -- ORDER:

KUBECTL LOGS

k='kubectl --kubeconfig kubeconfig-pro-log##fas####-##.yml'

$k get pods

$k get pods | command grep merch###log##fas#### | awk '{print $1}'

$k describe pod merch###log##fas###-846##6b###-s2### > out.describe.txt

$k logs merch###log##fas####-846##6b###-s2### --container merch###log##fas#### > out.log.txt


